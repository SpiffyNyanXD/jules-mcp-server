# Audit Report

- Fixed project name inconsistency (`wec-jules-server` to `jules-mcp-server` in `package.json`).
- Added missing files: `README.md`, `LICENSE`, `CONTRIBUTING.md`, `SECURITY.md`, `CHANGELOG.md`, `AGENTS.md`.
- Updated `.gitignore` to include standard ignore paths.
