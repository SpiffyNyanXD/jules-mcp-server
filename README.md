# jules-mcp-server

A Node.js Express application functioning as a Remote MCP Server for Google Jules.

## Setup

1. `npm install`
2. `npm start`

## Required Environment Variables

- `JULES_API_KEY`
