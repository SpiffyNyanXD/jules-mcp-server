# AI Coding Agent Instructions

- The project is a Node.js Express application that functions as a Remote MCP Server for Google Jules.
- It proxies requests to the Jules API via standard MCP over HTTP.
- Dependencies are managed via npm.
- The 'JULES_API_KEY' environment variable is strictly required.
- Supabase is used for persistence.
